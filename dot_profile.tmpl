. "$HOME/.cargo/env"

# Added by LM Studio CLI (lms)
export PATH="$PATH:{{ .chezmoi.homeDir }}/.lmstudio/bin"

# Added by LM Studio CLI (lms) - DUPLICATE ENTRY WITH TYPO FIXED
export PATH="$PATH:{{ .chezmoi.homeDir }}/.lmstudio/bin"
# End of LM Studio CLI section

